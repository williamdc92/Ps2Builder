# Ps2Builder
PS2 Builder creates a self-contained Windows game disc from a PS2 BIOS and game dump provided by the user.  The output is a single `.iso` file. When the ISO is mounted or burned to physical media, the end user simply opens **PLAY.exe** and the game launches directly using the PCSX2 runtime bundled inside the disc.
